---
import Layout from '../../layouts/Layout.astro';

const emplacementsPhotos = [
  { image: '/images/allee-caravanes-arbres.webp', alt: 'Allée de caravanes sous les arbres' },
  { image: '/images/emplacement-tente-ombrage.webp', alt: 'Emplacement de tente ombragé' },
  { image: '/images/sous-bois-tentes.webp', alt: 'Tentes installées sous les bois' },
  { image: '/images/tente-cloche-pins.webp', alt: 'Tente cloche installée sous les pins' },
];

const gitePhotos = [
  { image: '/images/gite-exterieur-terrasse.webp', alt: 'Terrasse extérieure du gîte' },
  { image: '/images/gite-jardin-transats.webp', alt: 'Jardin ombragé du gîte avec transats' },
  { image: '/images/gite-interieur-cuisine.webp', alt: 'Cuisine équipée du gîte' },
  { image: '/images/gite-interieur-couloir-sdb.webp', alt: 'Salle de bain privative du gîte' },
];

const sanitaires = [
  {
    image: '/images/camping-sanitaires-wc.webp',
    title: 'WC',
    caption: "Toilettes propres, entretenues quotidiennement.",
  },
  {
    image: '/images/camping-sanitaires-douche.webp',
    title: 'Douches',
    caption: 'Douches chaudes individuelles, accessibles à tous.',
  },
  {
    image: '/images/camping-sanitaires-lavabos.webp',
    title: 'Lavabos extérieurs',
    caption: "Bacs à vaisselle et lavabos à l'air libre, à l'ombre des pins.",
  },
  {
    image: '/images/camping-sanitaires-pmr.webp',
    title: 'Accès PMR',
    caption: 'Sanitaires adaptés aux personnes à mobilité réduite.',
  },
];

const receptionCards = [
  {
    icon: '🥖',
    label: 'Pain frais',
    text: 'Commandez votre pain la veille, à récupérer chaque matin à la réception.',
  },
  {
    icon: '💬',
    label: 'Informations',
    text: "Le point de repère du domaine pour vos questions et conseils sur les environs.",
  },
  {
    icon: '🌳',
    label: 'Espace convivial',
    text: 'Une terrasse ombragée pour se retrouver, à deux pas de l\'accueil.',
  },
];

const serviceIcons: Record<string, string> = {
  wifi: '<path d="M5 12.55a11 11 0 0 1 14.08 0"/><path d="M1.42 9a16 16 0 0 1 21.16 0"/><path d="M8.53 16.11a6 6 0 0 1 6.95 0"/><line x1="12" y1="20" x2="12.01" y2="20"/>',
  bbq: '<path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z"/>',
  playground: '<path d="M4 21V8"/><path d="M20 21V8"/><path d="M4 8h16"/><path d="M9 21l3-7 3 7"/><circle cx="12" cy="14" r="1" fill="currentColor"/>',
  paw: '<circle cx="7" cy="9" r="1.6"/><circle cx="12" cy="6.5" r="1.6"/><circle cx="17" cy="9" r="1.6"/><ellipse cx="12" cy="15.5" rx="5" ry="4"/>',
};

const services = [
  { icon: 'wifi', label: 'Wi-Fi' },
  { icon: 'bbq', label: 'Barbecue' },
  { icon: 'playground', label: 'Aire de jeux' },
  { icon: 'paw', label: 'Animaux acceptés' },
];
---

<Layout title="Séjourner — Camping Clairac">
  <section id="emplacements" class="section">
    <div class="section__inner--full">
      <h1>Les emplacements</h1>
      <p class="section__intro">
        25 emplacements pour tente, caravane ou camping-car, répartis dans
        un cadre calme et ombragé. Chaque emplacement est spacieux et
        espacé d'au moins 5 mètres de son voisin, pour profiter pleinement
        du plein air sans être les uns sur les autres.
      </p>

      <div class="emplacements-gallery">
        <button type="button" class="emplacements-gallery__main" data-emplacements-photo={0} aria-label="Agrandir la photo de l'emplacement">
          <img src={emplacementsPhotos[0].image} alt={emplacementsPhotos[0].alt} loading="lazy" />
        </button>
        <div class="emplacements-gallery__thumbs">
          {emplacementsPhotos.slice(1).map((photo, index) => (
            <button type="button" class="emplacements-gallery__thumb" data-emplacements-photo={index + 1} aria-label="Agrandir la photo de l'emplacement">
              <img src={photo.image} alt={photo.alt} loading="lazy" />
            </button>
          ))}
        </div>
      </div>
    </div>
  </section>

  <div class="lightbox" data-emplacements-lightbox>
    <button type="button" class="lightbox__close" data-emplacements-lightbox-close aria-label="Fermer">&times;</button>
    <button type="button" class="lightbox__arrow lightbox__arrow--prev" data-emplacements-lightbox-prev aria-label="Photo précédente">
      <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="15 18 9 12 15 6"></polyline>
      </svg>
    </button>
    <img src={emplacementsPhotos[0].image} alt="" class="lightbox__image" data-emplacements-lightbox-image />
    <button type="button" class="lightbox__arrow lightbox__arrow--next" data-emplacements-lightbox-next aria-label="Photo suivante">
      <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="9 18 15 12 9 6"></polyline>
      </svg>
    </button>
  </div>

  <section id="gite" class="section section--alt">
    <div class="section__inner">
      <div class="gite-gallery">
        <button type="button" class="gite-gallery__main" data-gite-photo={0} aria-label="Agrandir la photo du gîte">
          <img src={gitePhotos[0].image} alt={gitePhotos[0].alt} loading="lazy" />
        </button>
        <div class="gite-gallery__thumbs">
          {gitePhotos.slice(1).map((photo, index) => (
            <button type="button" class="gite-gallery__thumb" data-gite-photo={index + 1} aria-label="Agrandir la photo du gîte">
              <img src={photo.image} alt={photo.alt} loading="lazy" />
            </button>
          ))}
        </div>
      </div>
      <div class="section__text">
        <h2>Le gîte</h2>
        <p>
          Pas d'équipement à vous ? On a aussi une maison indépendante sur le
          domaine, pour ceux qui préfèrent poser leurs valises sans tente ni
          camping-car. On y trouve une cuisine équipée, une salle à manger et
          une salle de bain privative, tout le confort d'une vraie maison.
        </p>
        <p>
          Dehors, un jardin ombragé avec des transats invite à ne rien faire,
          dans le même calme que le reste du domaine.
        </p>
        <a href="/infos-pratiques/contact" class="btn btn--primary">Nous contacter pour réserver</a>
      </div>
    </div>
  </section>

  <div class="lightbox" data-gite-lightbox>
    <button type="button" class="lightbox__close" data-gite-lightbox-close aria-label="Fermer">&times;</button>
    <button type="button" class="lightbox__arrow lightbox__arrow--prev" data-gite-lightbox-prev aria-label="Photo précédente">
      <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="15 18 9 12 15 6"></polyline>
      </svg>
    </button>
    <img src={gitePhotos[0].image} alt="" class="lightbox__image" data-gite-lightbox-image />
    <button type="button" class="lightbox__arrow lightbox__arrow--next" data-gite-lightbox-next aria-label="Photo suivante">
      <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="9 18 15 12 9 6"></polyline>
      </svg>
    </button>
  </div>

  <section id="sanitaires" class="section">
    <div class="section__inner--full">
      <h2>Sanitaires</h2>
      <div class="sanitaires-grid">
        {sanitaires.map((item) => (
          <div class="sanitaire-card">
            <img src={item.image} alt={item.title} loading="lazy" class="sanitaire-card__image" />
            <div class="sanitaire-card__body">
              <p class="sanitaire-card__title">{item.title}</p>
              <p class="sanitaire-card__caption">{item.caption}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="reception" class="section section--alt">
    <div class="section__inner--full">
      <h2>La réception</h2>
      <p class="section__intro">
        À votre arrivée comme tout au long de votre séjour, la réception est
        le point de repère du domaine : accueil, informations et un service
        de pain frais pour bien commencer la journée.
      </p>
      <img
        src="/images/camping-reception.webp"
        alt="Accueil et réception du camping"
        class="reception-photo"
        loading="lazy"
      />
      <div class="reception-grid">
        {receptionCards.map((card) => (
          <div class="reception-card">
            <div class="reception-card__icon" aria-hidden="true">{card.icon}</div>
            <p class="reception-card__label">{card.label}</p>
            <p class="reception-card__text">{card.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="services" class="section">
    <div class="section__inner--full">
      <h2>Services</h2>
      <div class="services-grid">
        {services.map((service) => (
          <div class="service-card">
            <div class="service-card__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" set:html={serviceIcons[service.icon]} />
            </div>
            <p class="service-card__label">{service.label}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="tarifs" class="section pricing">
    <div class="pricing__inner">
      <h2>Tarifs</h2>
      <p>Des tarifs simples et transparents pour tous les types d'emplacements.</p>
      <a href="/sejourner/tarifs" class="btn btn--primary">Voir le détail des tarifs</a>
    </div>
  </section>
</Layout>

<style>
  .section {
    padding: 4rem 1.5rem;
  }

  .section--alt {
    background-color: var(--color-sand);
  }

  .section__inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2.5rem;
    align-items: center;
  }

  .section__inner--full {
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
  }

  .section__intro {
    max-width: 700px;
    margin: 0 auto 2.5rem;
  }

  .section__inner--full h1,
  .section__inner--full h2 {
    margin-bottom: 1rem;
  }

  .section__text h2 {
    margin-bottom: 1rem;
  }

  .section__text p {
    margin-bottom: 1rem;
  }

  .btn {
    display: inline-block;
    padding: 0.85rem 1.75rem;
    border-radius: 999px;
    font-weight: 600;
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .btn--primary {
    background-color: var(--color-forest);
    color: var(--color-cream);
  }

  .btn--primary:hover {
    background-color: var(--color-sage);
  }

  /* Emplacements gallery + Gîte gallery share the same structure */
  .emplacements-gallery {
    max-width: 700px;
    margin: 0 auto;
  }

  .emplacements-gallery,
  .gite-gallery {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .emplacements-gallery__main,
  .emplacements-gallery__thumb,
  .gite-gallery__main,
  .gite-gallery__thumb {
    display: block;
    padding: 0;
    border: none;
    background: none;
    cursor: zoom-in;
    border-radius: 12px;
    overflow: hidden;
  }

  .emplacements-gallery__main img,
  .gite-gallery__main img {
    width: 100%;
    height: 320px;
    object-fit: cover;
    display: block;
  }

  .emplacements-gallery__thumbs,
  .gite-gallery__thumbs {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.75rem;
  }

  .emplacements-gallery__thumb img,
  .gite-gallery__thumb img {
    width: 100%;
    height: 100px;
    object-fit: cover;
    display: block;
    transition: opacity 0.2s ease;
  }

  .emplacements-gallery__thumb:hover img,
  .emplacements-gallery__main:hover img,
  .gite-gallery__thumb:hover img,
  .gite-gallery__main:hover img {
    opacity: 0.85;
  }

  /* Lightbox */
  .lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    background-color: rgba(36, 56, 44, 0.9);
  }

  .lightbox.active {
    display: flex;
  }

  .lightbox__image {
    max-width: calc(100% - 8rem);
    max-height: 100%;
    border-radius: 8px;
  }

  .lightbox__close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background-color: rgba(244, 241, 232, 0.15);
    color: var(--color-cream);
    font-size: 1.75rem;
    line-height: 1;
    border: none;
    cursor: pointer;
  }

  .lightbox__close:hover {
    background-color: rgba(244, 241, 232, 0.3);
  }

  .lightbox__arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background-color: rgba(244, 241, 232, 0.15);
    color: var(--color-cream);
    border: none;
    cursor: pointer;
    transition: background-color 0.2s ease;
  }

  .lightbox__arrow:hover {
    background-color: rgba(244, 241, 232, 0.3);
  }

  .lightbox__arrow--prev {
    left: 1.5rem;
  }

  .lightbox__arrow--next {
    right: 1.5rem;
  }

  /* Sanitaires infographic grid */
  .sanitaires-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.5rem;
    text-align: left;
  }

  .sanitaire-card {
    background-color: #fff;
    border: 1px solid var(--color-sand);
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 4px 16px rgba(36, 56, 44, 0.06);
    display: flex;
    flex-direction: column;
  }

  .sanitaire-card__image {
    width: 100%;
    height: 160px;
    object-fit: cover;
  }

  .sanitaire-card__body {
    padding: 1.25rem;
    flex: 1;
  }

  .sanitaire-card__title {
    font-family: var(--font-heading);
    font-weight: 600;
    color: var(--color-forest);
    margin: 0 0 0.4rem;
  }

  .sanitaire-card__caption {
    margin: 0;
    font-size: 0.9rem;
  }

  /* Réception section */
  .reception-photo {
    display: block;
    width: 100%;
    max-width: 700px;
    height: 280px;
    object-fit: cover;
    border-radius: 12px;
    margin: 0 auto 2rem;
  }

  .reception-grid {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.5rem;
  }

  .reception-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.6rem;
    background-color: #fff;
    border: 1px solid var(--color-sand);
    border-radius: 16px;
    padding: 1.75rem 1.25rem;
    box-shadow: 0 4px 16px rgba(36, 56, 44, 0.06);
  }

  .reception-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background-color: rgba(104, 122, 90, 0.15);
    font-size: 2rem;
    line-height: 1;
  }

  .reception-card__label {
    margin: 0;
    font-family: var(--font-heading);
    font-weight: 600;
    color: var(--color-forest);
  }

  .reception-card__text {
    margin: 0;
    font-size: 0.9rem;
  }

  /* Services grid — same card format as "Notre démarche nature" */
  .services-grid {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.5rem;
  }

  .service-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.75rem;
    background-color: #fff;
    border: 1px solid var(--color-sand);
    border-radius: 16px;
    padding: 2rem 1.5rem;
    box-shadow: 0 4px 16px rgba(36, 56, 44, 0.06);
  }

  .service-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background-color: rgba(104, 122, 90, 0.15);
    color: var(--color-forest);
  }

  .service-card__label {
    margin: 0;
    font-weight: 600;
    color: var(--color-forest);
  }

  .pricing {
    text-align: center;
  }

  .pricing__inner {
    max-width: 600px;
    margin: 0 auto;
  }

  .pricing__inner h2 {
    margin-bottom: 0.75rem;
  }

  .pricing__inner p {
    margin-bottom: 1.5rem;
  }

  @media (max-width: 860px) {
    .section__inner {
      grid-template-columns: 1fr;
    }

    .sanitaires-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .services-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .reception-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .sanitaires-grid {
      grid-template-columns: 1fr;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .emplacements-gallery__thumb img,
    .gite-gallery__thumb img {
      height: 80px;
    }

    .lightbox__arrow {
      width: 38px;
      height: 38px;
    }

    .lightbox__arrow--prev {
      left: 0.5rem;
    }

    .lightbox__arrow--next {
      right: 0.5rem;
    }

    .lightbox__image {
      max-width: calc(100% - 3rem);
    }
  }
</style>

<script>
  // Reusable setup for a "main photo + thumbnails" gallery with its lightbox
  function setupGallery(prefix: string) {
    const photoData = Array.from(document.querySelectorAll<HTMLElement>(`[data-${prefix}-photo]`)).map((btn) => {
      const img = btn.querySelector('img');
      return { src: img?.getAttribute('src') ?? '', alt: img?.getAttribute('alt') ?? '' };
    });

    const lightbox = document.querySelector<HTMLElement>(`[data-${prefix}-lightbox]`);
    const lightboxImage = document.querySelector<HTMLImageElement>(`[data-${prefix}-lightbox-image]`);
    const lightboxClose = document.querySelector<HTMLElement>(`[data-${prefix}-lightbox-close]`);
    const lightboxPrev = document.querySelector<HTMLElement>(`[data-${prefix}-lightbox-prev]`);
    const lightboxNext = document.querySelector<HTMLElement>(`[data-${prefix}-lightbox-next]`);
    const photoButtons = Array.from(document.querySelectorAll<HTMLElement>(`[data-${prefix}-photo]`));

    if (!lightbox || !lightboxImage || photoData.length === 0) return;

    let currentIndex = 0;

    const showPhoto = (index: number) => {
      const total = photoData.length;
      currentIndex = (index + total) % total;
      const photo = photoData[currentIndex];
      lightboxImage.src = photo.src;
      lightboxImage.alt = photo.alt;
    };

    const openLightbox = (index: number) => {
      showPhoto(index);
      lightbox.classList.add('active');
    };

    const closeLightbox = () => {
      lightbox.classList.remove('active');
    };

    photoButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const index = Number(btn.getAttribute(`data-${prefix}-photo`) ?? '0');
        openLightbox(index);
      });
    });

    lightboxClose?.addEventListener('click', closeLightbox);
    lightboxPrev?.addEventListener('click', () => showPhoto(currentIndex - 1));
    lightboxNext?.addEventListener('click', () => showPhoto(currentIndex + 1));

    lightbox.addEventListener('click', (e) => {
      if (e.target === lightbox) closeLightbox();
    });

    document.addEventListener('keydown', (e) => {
      if (!lightbox.classList.contains('active')) return;
      if (e.key === 'Escape') closeLightbox();
      if (e.key === 'ArrowLeft') showPhoto(currentIndex - 1);
      if (e.key === 'ArrowRight') showPhoto(currentIndex + 1);
    });
  }

  setupGallery('gite');
  setupGallery('emplacements');
</script>
